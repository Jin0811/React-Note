/**
 * 该模块是用于定义action对象中type类型的常用值，目的只有一个：便于管理的同时防止拼写错误
 */

export const INCREMENT = "increment"; // 增加
export const DECREMENT = "decrement"; // 减少

export const ADD_PERSON = "add_person"; // 添加人员